module Wrasse.Test where
-- -- Objective: fix the type error in this file

-- -- Insert an element at a given position into a list.

-- insertAt el lst n =
--     let accu (i, acc) x =
--             if i == n
--                 then (acc ++ [el,x],i+1)
--                 else (acc ++ [x],i+1)
--     in fst $ foldl accu ([],1) lst


